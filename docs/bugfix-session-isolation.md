# Bug ?????Session ???????2026-06-22?

## ??

1. ???? session ????????? session ? pending ??
2. ? session ????? HTML ???? session ????
3. ??????? HTML ????????

## ????

### Bug 1: GET /api/tasks ?? session ??

`server.mjs:2477-2480` ? `GET /api/tasks` ???? `readTasks()` ??????
??? session????? session ???? tasks ?????? session ????

### Bug 2: dispatch ? session ????

`server.mjs:3272` ? `dispatch` ? `readTasks()` ???? pending ???
???? session ? pending ??????????

### Bug 3: ?? loadTasks ?? sessionId

`web/app.js:1040-1041` ? `loadTasks()` ???? `/api/tasks` ????
???????????????????????

## ????

### ?? 1: server.mjs - GET /api/tasks ?? sessionId ??

```diff
- GET /api/tasks
+ GET /api/tasks?sessionId=xxx

- return res.end(JSON.stringify({ tasks: readTasks() }));
+ const taskSid = url.searchParams.get('sessionId') || '';
+ const allTasks = readTasks();
+ const tasks = taskSid ? allTasks.filter(t => t.session_id === taskSid) : allTasks;
+ return res.end(JSON.stringify({ tasks }));
```

### ?? 2: server.mjs - dispatch ???? session

```diff
- let pending = readTasks().filter(t => t.status === 'pending');
+ let allTasks = readTasks();
+ if (dispatchSession) {
+   allTasks = allTasks.filter(t => t.session_id === dispatchSession.id);
+ }
+ let pending = allTasks.filter(t => t.status === 'pending');
```

### ?? 3: web/app.js - ??? sessionId ?? tasks

```diff
- const { tasks } = await fetch('/api/tasks').then(r => r.json())...
+ const sid = currentSessionId || '';
+ const url = '/api/tasks' + (sid ? '?sessionId=' + encodeURIComponent(sid) : '');
+ const { tasks } = await fetch(url).then(r => r.json())...
```

## ??

- `GET /api/tasks` ? ?? 15 ???????????
- `GET /api/tasks?sessionId=86ef514f` ? ?? 11 ????? session?
- `GET /api/tasks?sessionId=c9e0fb48` ? ?? 4 ??AI ?? session?
- dispatch ????? session ? pending ??

## ????

- server.mjs: L2477-2484, L3275-3278
- web/app.js: L1040-1042
