# myteamOUO 修复交接文档

> Commit: P0–P2 fixes for task lifecycle, env isolation, and reviewer UX
> Date: 2026-06-24
> Tests: **53/53** 全部通过

---

## 修复清单（7 个问题，7 个文件）

### P0 · halt() 丢弃剩余任务
**文件**: `workflow/dispatch-graph.mjs`
**问题**: 队列中任一任务失败后，`halt` 节点直接 `END`，后续 pending 任务永远不会执行。
**修复**:
- `halt` 不再 `END`，改为 `halt → select_task`
- 失败任务计入 `failedTaskIds`，cursor 推进，剩余任务继续执行
- 队列跑完后 `finish` 输出 `completed_with_errors`
- 测试已更新：`t1` reviewer 失败后 `t2` 正常执行

### P1 · 进程环境变量泄露
**文件**: `agent-utils.mjs`
**问题**: `buildAgentSpawnEnv` 复制全部 `process.env` 再删除白名单变量，但 VSCode IDE 注入的 20+ 个 `ANTHROPIC_*` / `CODEX_*` 变量仍会泄露给子进程，导致 token 超限和认证冲突。
**修复**:
- 改为白名单模式：空对象起手，只复制 `PATH / HOME / TEMP / SystemRoot / APPDATA` 等系统必需变量
- 代码从 20 行冲突删除 → 14 行精确保留，更安全且更简洁

### P1 · 服务启动即崩溃
**文件**: `prompts.mjs`
**问题**: `REVIEW_PROMPT_RULES` 中包含 `第一个字符必须是 {`，LangChain `ChatPromptTemplate.fromTemplate()` 把 `{` 解析为模板变量，启动时抛出 `INVALID_PROMPT_INPUT: Missing value for input variable「，最后一个字符必须是」`。
**修复**:
- `{` → `左大括号`，`}` → `右大括号`，避免 LangChain 模板误解析

### P1 · rerun API 状态转换不彻底
**文件**: `server.mjs`
**问题**: failed 任务通过 `/api/tasks/:id/rerun` 重置时，`failure_stage` / `retryable` / `interruption_reason` / `interrupted_at` 等字段未被清理，导致前端误判任务不可重试。
**修复**:
- `patchDefaults` 扩展覆盖 `failure_stage: null / retryable: null / interruption_reason: null / interrupted_at: null`
- `synchronizeTaskRecord` 也同样补全这些字段

### P1 · Reviewer 选择策略不合理
**文件**: `server.mjs`
**问题**: `runAutoReview` 的 reviewer 选择策略是 `statuses.find(a => a.key !== executor) || 自验收`。当有 3 个可用 agent 时，codex 排在第一位就会被选为 reviewer，但 codex 在设计上只负责规划/协调，不应兼任验收。
**修复**:
- 有多个可用 agent（非 executor）时，优先选 `claude`，其次排除 `codex`
- 只有一个可用 agent 时才允许自验收或被迫选 codex

### P2 · 验收阶段用户交互感知弱
**文件**: `web/app.js` / `web/app.css`
**问题**: task 进入 review 阶段后，前端只有工作流卡片上一行小字，用户看不到明确的验收进度。等 reviewer 完成才有结果卡片出现。
**修复**:
- `task-review-start` / `task-review-resume` 事件触发时，聊天区立即插入「正在验收中」卡片（蓝色左边框 + 脉冲图标 + 滚动进度条）
- `task-review-done` 事件触发时，用 `trc-{taskId}` 前缀 id 替换掉验收中卡片，避免重复展示
- CSS 新增 70 行：`.task-review-chat-card.reviewing` / `@keyframes reviewPulse` / `@keyframes reviewBar`

---

## 文件变更统计

```
 agent-utils.mjs                   | 45 ++++------------
 prompts.mjs                       |  2 +-
 server.mjs                        | 65 ++++++++++++++--------
 tests/langgraph-workflow.test.mjs | 12 ++---
 web/app.css                       | 74 +++++++++++++++++++++++
 web/app.js                        | 47 ++++++++++++---
 workflow/dispatch-graph.mjs       | 14 +++--
 7 files, 186 insertions, 73 deletions
```

---

## 测试结果

| 测试套件 | 通过 | 失败 |
|---------|------|------|
| langgraph-workflow.test.mjs | 8 | 0 |
| workflow-state.test.mjs | 6 | 0 |
| e2e-lifecycle.test.mjs | 2 | 0 |
| collaboration-context.test.mjs | 13 | 0 |
| traceability.test.mjs | 16 | 0 |
| product-guidance.test.mjs | 3 | 0 |
| p6-p8.test.mjs | 5 | 0 |
| **总计** | **53** | **0** |

---

## 已知问题（未修复）

- **Codex CLI 403**: Codex CLI 0.138.0 忽略 `config.toml` 的 `base_url`，始终硬编码连接 `wss://api.openai.com/v1/responses`。CC Switch 本地代理（`127.0.0.1:15720`）本身工作正常（curl 测试通过），但 CLI 不走代理。需联系 CC Switch 团队或等 Codex CLI 更新。
- **orphan checkpoint cleanup 报错** `no such table: workflow_adapters`：启动日志中的 WARNING，不影响功能，属于 SQLite migration 时序问题。
